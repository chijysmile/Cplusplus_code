# C++ STL vector 全面剖析：从基本使用到源码实现

## 一、vector 概述
- 什么是 vector：C++ STL 中的动态数组（连续内存存储）
- 为什么使用 vector：相比原生数组支持动态扩容、自动管理内存
- 基本声明方式：`vector<int>`、`vector<vector<int>>`
- 常用头文件与命名空间

## 二、vector 的底层结构
- 三个核心成员变量：
  - `int* _a`（数据指针，指向堆上连续内存）
  - `size_t _size`（当前有效元素个数）
  - `size_t _capacity`（当前容量，可容纳的最大元素数）
- `size()` 与 `capacity()` 的区别与关系
- 内存布局示意图：`size ≤ capacity`，多余容量用于后续插入

## 三、手写模拟实现简易 vector
- 类的基本框架：`class vector { ... }`
- 下标访问 `operator[]` 的实现：
  - 边界检查：`assert(i < _size)`
  - 返回元素：`return _a[i]`
- 返回类型 `int&` 的意义：可读可写（`vv[2][1] = 2` 才能生效）
- 私有成员设计：`int* _a; size_t _size; size_t _capacity;`

## 四、二维数组：vector\<vector\<int\>\>
- 声明与初始化（10 行 × 5 列示例）：
  - `vector<int> v(5, 1);`
  - `vector<vector<int>> vv(10, v);`
- 元素访问与修改：`vv[2][1] = 2;`
- 底层访问过程剖析：
  - `vv[2]` 返回 `vector<int>&`（一行的引用）
  - `vv[2][1]` 返回 `int&`（具体元素的引用）
- 与"数组的数组"内存模型的对应关系

## 五、C 风格指针数组模拟二维数组
- 行指针分配：`int** pp = malloc(sizeof(int*) * 10)`
- 逐行分配列：`pp[i] = malloc(sizeof(int) * (i + 1))`
- 二维数组长度（行数）与每一行长度（列数）的管理
- 与 LeetCode 中 `returnColumnSizes` 参数的对应关系
- 使用完手动 `free` 的注意事项（对比 vector 的自动释放）

## 六、实战演练：LeetCode 118 杨辉三角
- 题目描述与解题思路
- vector 版本实现：
  - `vector<vector<int>> vv(numRows);`
  - `for (size_t i = 0; i < numRows; ++i) vv[i].resize(i + 1, 1);`
  - 边界初始化技巧：`vv[i].front() = vv[i].back() = 1`
  - 核心递推公式：`vv[i][j] = vv[i-1][j-1] + vv[i-1][j]`
  - `return vv;`
- C 语言 `int**` 版本实现对比（`generate(int numRows, int* returnSize, int** returnColumnSizes)`）
- 两种实现方式的优缺点对比

## 七、深入源码：侯捷《STL源码剖析》中的 vector
- 真正的 STL vector 底层三指针结构：
  - `start`（指向第一个元素）
  - `finish`（指向最后一个元素的下一个位置，即 size 所在）
  - `end_of_storage`（指向容量末尾）
- `push_back(const T& x)` 完整流程：
  - `if (finish != end_of_storage)`：空间充足，直接构造
    - `construct(finish, x); ++finish;`
  - `else`：空间不足，调用 `insert_aux(end(), x)` 触发扩容
- `construct` 与 placement new：
  - `template <class T1, class T2> void construct(T1* p, const T2& value)`
  - `new (p) T1(value);` 在指定内存位置构造对象
- `insert_aux` 扩容机制：分配新空间 → 搬移旧元素 → 释放旧空间
- 模板定义：`template <class T, class Alloc> class vector`

## 八、总结与延伸
- 面试高频考点：
  - `size` vs `capacity` 的区别
  - vector 的扩容策略（何时扩容、如何搬移）
  - 二维数组的三种实现方式对比（`vector<vector<int>>`、指针数组、一维模拟）
- 延伸阅读：侯捷《STL源码剖析》、`deque`/`list` 与 vector 的对比
- 练习建议：手写简易 vector、完成杨辉三角、理解扩容的复杂度分析